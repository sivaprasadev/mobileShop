<section>

    <div class="container p-auto">
        <h1 class="text-center mt-5 display-4">New Arrivals</h1>
        
        <div class="row">
            {{#each products}}
            <div class="col-md-3 mt-5">
                <div class="card mt-0" style="width:100%;height:100%">
                    <img class="card-img-top" src="/product-images/{{this._id}}.jpg" alt="Card image cap"
                        style="height:150px;width:254px">
                    <div class="card-body ">
                        <h5 class="card-title">{{this.Name}}</h5>
                        <h5 class="card-title">Rs.{{this.Price}}</h5>
                        <p class="card-text">{{this.Description}}</p>
                        <!--<a href="/add-to-cart/{{this._id}}" class="btn btn-primary">Add to Cart</a>-->
                    </div>
                    <button onclick="addToCart('{{this._id}}')" class="btn btn-primary rounded-0">Add to cart</button>
                </div>

            </div>
            {{/each}}
        </div>

    </div>
</section>

</body>